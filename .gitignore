# Gitignored per project — see docs/operations/local-workspace-layout.md
.local/
.local/**

# Runtime / secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
coverage.json
htmlcov/
.venv/
venv/

# Research (optional overlay)
_research_results/

# Local install dry-run / scratch trees (never commit — may contain .env, nested .git)
_temp_test_installation/
_temp_test_installation/**
_temp_kit_installation/
_temp_kit_installation/**
_customer_installation_test/
_customer_installation_test/**

# Plugin bundle: agents/, rules/, skills/, payload/ are generated by
# `make sync-plugin` but MUST be committed — Cursor Marketplace reads the
# git tree directly (no build step). `make check-plugin` guards drift.

# Editor
.DS_Store
.cursor/mcp.user.json
.local/user_settings/mcp.secrets.yaml
