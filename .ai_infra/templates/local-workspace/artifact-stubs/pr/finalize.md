# PR Finalize

**Status:** Not yet written.

Run:
- `/full-pr-workflow` (when you want repo cleanup + branch deletion), or
- `python .ai_infra/scripts/pr/finalize.py --branch <feature-branch>`

This tab is overwritten by `finalize.py` after cleanup runs.

